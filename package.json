{
  "name": "cloudflare-markdown-mcp-server",
  "version": "1.0.0",
  "description": "Cloudflare AI Markdown変換APIをClaude Code/CodexなどのAIエージェントから使えるMCPサーバー",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "agents": "^0.3.10",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.71.0"
  }
}
